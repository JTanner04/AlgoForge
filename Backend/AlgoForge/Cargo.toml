[package]
name = "AlgoForge"
version = "0.1.0"
edition = "2024"

[dependencies]
axum = "0.8.8"
sqlx = { version = "0.7", features = ["postgres", "runtime-tokio-rustls", "macros", "migrate"]}
tokio = { version = "1", features = ["full"]}
dotenv = "0.15"
